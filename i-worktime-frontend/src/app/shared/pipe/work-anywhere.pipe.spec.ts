import { WorkAnywherePipe } from './work-anywhere.pipe';

describe('WorkAnywherePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkAnywherePipe();
    expect(pipe).toBeTruthy();
  });
});
